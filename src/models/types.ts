export type Dashboard = { orders: number; pendingOrders: number; completedOrders: number; cancelledOrders: number; customers: number; organizations: number; products: number; returns: number; refunds: number; lowStockProducts: number; inventory: { available: number; reserved: number }; revenue: string; todaySales: string; monthlySales: string; currency: string };
export type AdminOrder = { id: string; status: string; total: string; currency: string; createdAt: string; user: { email: string; firstName: string; lastName: string }; items: Array<{ quantity: number }> };
export type Organization = { id: string; name: string; status: string; memberships: Array<{ role: string; user: { email: string } }> };
export type Page<T> = { items: T[]; pagination: { page: number; pageSize: number; total: number; totalPages: number } };
export type AuthResponse = { user: { email: string; type: string }; tokens: { accessToken: string } };
export type AdminProduct = { id: string; sku: string; name: string; status: string; category: { name: string }; inventories: Array<{ available: number; reserved: number }>; images: Array<{ id: string; url: string; altText: string | null; sortOrder: number }> };
export type AdminInventory = { id: string; product: { name: string; sku: string }; variant?: { name: string } | null; available: number; reserved: number; damaged: number; returned: number };
export type PriceTier = { id: string; productId: string; type: string; amount: string; minimumQty: number; maximumQty?: number | null; product: { name: string; sku: string } };
export type PromoCode = { id: string; code: string; type: string; value: string; customerType: string; active: boolean; usageCount: number; expiresAt: string };
export type ProductForm = { sku: string; name: string; slug: string; categoryId: string; status: 'ACTIVE' | 'INACTIVE' };
export type InventoryAdjustmentForm = { productId: string; quantity: string; type: string };
export type PriceTierForm = { productId: string; type: string; amount: string; minimumQty: string; maximumQty: string };
export type PromoCodeForm = { code: string; type: string; value: string; startsAt: string; expiresAt: string; customerType: string };
export type AdminUser = { id: string; email: string; firstName: string; lastName: string; type: string; status: string; lastLoginAt: string | null; createdAt: string; roles: Array<{ role: { id: string; name: string } }> };
export type CreateUserForm = { email: string; password: string; firstName: string; lastName: string; type: 'ADMIN' | 'CUSTOMER' };
export type AdminRole = { id: string; name: string; description: string | null; permissions: Array<{ permission: { id: string; resource: string; action: string } }> };
export type AuditLog = { id: string; action: string; entity: string; entityId: string | null; createdAt: string; actor: { email: string } | null; organization: { name: string } | null };
export type AdminReturn = { id: string; status: string; reason: string; createdAt: string; user: { email: string; firstName: string; lastName: string }; order: { id: string; total: string; currency: string }; items: Array<{ quantity: number; orderItem: { productName: string } }>; refund: { amount: string; currency: string } | null };
export type AdminPurchaseOrder = { id: string; number: string; status: string; amount: string; dueAt: string | null; createdAt: string; organization: { name: string }; requestedBy: { email: string; firstName: string; lastName: string }; order: { total: string; currency: string; status: string } };
export type AdminInvoice = { id: string; number: string; issuedAt: string; order: { total: string; currency: string; status: string; user: { email: string } } };
export type AdminPrivacyRequest = { id: string; type: string; status: string; note: string | null; createdAt: string; processedAt: string | null; user: { email: string; firstName: string; lastName: string } };
export type AdminCategory = { id: string; name: string; slug: string; description: string | null; parentId: string | null };
export type CategoryForm = { name: string; slug: string; description: string; parentId: string };
export type AdminBrand = { id: string; name: string; slug: string };
export type BrandForm = { name: string; slug: string };
export type AdminPayment = { id: string; provider: string; status: string; amount: string; currency: string; providerRef: string | null; createdAt: string; order: { id: string; user: { email: string } } };
export type TaxRule = { id: string; name: string; rate: string; country: string; region: string | null; active: boolean };
export type TaxRuleForm = { name: string; rate: string; country: string; region: string };
export type ShippingRule = { id: string; zoneName: string; country: string; region: string | null; rate: string; freeShippingThreshold: string | null; estimatedDaysMin: number; estimatedDaysMax: number; active: boolean };
export type ShippingRuleForm = { zoneName: string; country: string; region: string; rate: string; freeShippingThreshold: string; estimatedDaysMin: string; estimatedDaysMax: string };
export type SystemSetting = { key: string; value: unknown; updatedAt: string };
export type AdminReview = { id: string; rating: number; title: string | null; body: string; approved: boolean; reported: boolean; reportReason: string | null; createdAt: string; user: { email: string }; product: { name: string; sku: string } };
export type PolicyVersion = { id: string; version: number; content: string; published: boolean; effectiveAt: string; createdAt: string };
export type AdminPolicy = { id: string; type: string; title: string; versions: PolicyVersion[] };
export type PolicyVersionForm = { type: string; title: string; content: string; effectiveAt: string };
export type DashboardTrendPoint = { date: string; orders: number; revenue: number; b2bOrders: number; b2bRevenue: number; returns: number; refunds: number };
export type DashboardTrends = { days: number; series: DashboardTrendPoint[] };
export type AdminNotification = { id: string; type: string; title: string; body: string; createdAt: string; readAt: string | null; user: { email: string } };
export type BroadcastForm = { type: string; title: string; body: string; allCustomers: boolean };
